# Model weights (not in git)

Place `car_damage_r101_dc5.pth` (~765 MB) here after cloning:

```bash
python scripts/fetch_weights.py          # local sibling paths or Hugging Face
# or download the asset from the GitHub Release into this folder
```

This directory is gitignored. Download the checkpoint from the GitHub Release
into this folder, or run `python scripts/fetch_weights.py`.
